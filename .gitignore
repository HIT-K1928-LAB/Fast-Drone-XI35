build
devel
.catkin_workspace
.vscode
log

# Local TensorRT archives must be downloaded manually and are too large for Git.
Docker/TensorRT-*.tar.gz
Docker/TensorRT-*.tar.gz:Zone.Identifier

# Local TensorRT plugin build outputs.
libcustom_layernorm.so
